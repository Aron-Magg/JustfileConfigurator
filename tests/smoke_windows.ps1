# smoke_windows.ps1 — Windows smoke test (DOCUMENTED PLACEHOLDER).
#
# The Unix-first template ships working Arch/Debian/macOS support. This test is
# a stub: enable it on a Windows CI runner with `just` + PowerShell installed,
# mirroring tests/smoke_unix.sh (just --list, platform, health-all, config, …).
# The configurator's validator reports this as a deferred check until then.

Write-Host "[skipped] Windows smoke test is a placeholder — enable on a Windows CI runner."
exit 0
