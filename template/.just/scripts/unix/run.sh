#!/usr/bin/env bash
set -euo pipefail
. "$(dirname "$0")/lib.sh"

info "Running project (platform: $(detect_platform))"
# TODO: replace with your application's entry point.
ok "Nothing to run yet — edit .just/scripts/unix/run.sh"
